version: 2.1
jobs:
  build:
    docker: 
      - image: circleci/openjdk:8u242-jdk
    steps:
      - checkout
      - setup_remote_docker
      - run: mvn clean package
      - run: ls -la target
      - run: docker ps -a

